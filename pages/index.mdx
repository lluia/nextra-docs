import { Callout } from "nextra/components";
import { Link } from "../components/Link/Link";
import { Tooltip } from "../components/Tooltip/Tooltip";
import { ListDisclosure } from "../components/ListDisclosure/ListDisclosure";
import manifest from "./data/manifest.json";

# About Auth.js

Auth.js is a complete open-source authentication solution for web applications.<br />Check out the live demos of Auth.js in action:

<div className="flex flex-row justify-around w-full mt-8 mb-12">
  {[
    {
      href: "https://next-auth-example.vercel.app/",
      img: "https://authjs.dev/img/frameworks/nextjs.svg",
      name: "Next.js",
      logoWidth: "40",
      wip: false,
      label: ""
    },
    {
      href: "https://sveltekit-auth-example.vercel.app/",
      img: "https://authjs.dev/img/frameworks/sveltekit.svg",
      name: "Sveltekit",
      logoWidth: "35",
      wip: true,
      label: "Officially supported but we need to update the documentation with code examples. Help us document it."
    },
    {
      href: "https://auth-solid.vercel.app/",
      img: "https://authjs.dev/img/frameworks/solidstart.svg",
      name: "SolidStart",
      logoWidth: "45",
      wip: true,
      label: "Officially supported but we need to update the documentation with code examples. Help us document it"
    },
  ].map(({ href, name, img, logoWidth, wip, label }) => {
    const content = (
      <Link
        href={href}
        key={name}
        className="p-4 border border-solid border-slate-200 rounded-lg flex flex-col items-center justify-between w-28 shadow-lg relative"
        target="_blank"
      >
        <img src={img} width={logoWidth} />
        <div className="text-sm mt-3">{name}</div>
        {wip ? (
          <div
            className="absolute px-3 py-1 font-semibold text-sm bg-amber-300 text-black rounded-full shadow-sm"
            style={{ right: "-30px", top: "-15px" }}
          >
            Beta
          </div>
        ) : null}
      </Link>
    )

return wip ? <Tooltip label={label}>{content}</Tooltip> : content
})}

</div>

Support for more frameworks are on the way, check [our roadmap](/reference#roadmap)!

## NextAuth.js → Auth.js

NextAuth.js has been re-architected and rebranded as **Auth.js**. NextAuth.js was initially born as an authentication library for [Next.js](https://nextjs.org/). With v5 we took a step back and refactored the whole library to be an authentication layer which is framework agnostic.

<img
  src="/img/etc/authjs-architecture.png"
  width="500"
  className="mx-auto mt-4"
/>

Auth.js stays as a core JS authentication layer which can be used in any
framework (using the specific package for the framework). See
[Installation](/installation) for more details.

<Callout type="info">
  Migrating from v4? See [our migration guide](/migrating-to-v5).
</Callout>

## Authentication methods

There are 3 ways to authenticate users with Auth.js:

- [OAuth authentication](/getting-started/providers/oauth-tutorial) ( _Sign in with Google, Github, LinkedIn, etc..._ )
- [Paswordless authentication](/getting-started/providers/email-tutorial) ( _Magic links, OTP codes, WebAuthn, etc..._ )
- [External Auth](/getting-started/providers/credentials-tutorial) ( _Integrating with external APIs, etc..._ )

Additionally, Auth.js can be integrated with an external database via Database adapters, in case you need or want to store user data.

### OAuth Providers

<ListDisclosure limit={8}>
  {Object.entries(manifest.providers).map(([id, name]) => (
    <Link
      href={`/reference/core/providers/${id}`}
      key={name}
      className="p-4 border border-solid border-slate-200 rounded-lg flex flex-col items-center justify-between w-36 shadow-lg h-32"
    >
      <img src={`/img/providers/${id}.svg`} className="w-11 mt-2" />
      <div class="text-sm text-center">{name}</div>
    </Link>
  ))}
</ListDisclosure>

### Supported Databases

<ListDisclosure limit={8}>
  {Object.entries(manifest.adapters).map(([id, [name, img]]) => (
    <Link
      href={`/reference/adapter/${id}`}
      key={id}
      className="p-4 border border-solid border-slate-200 rounded-lg flex flex-col items-center justify-between w-36 shadow-lg h-32"
    >
      <img src={`/img/adapters/${img || `${id}.svg`}`} className="h-12 mt-2" />
      <div class="text-sm text-center">{name}</div>
    </Link>
  ))}
</ListDisclosure>

## Next steps

Ready to get started? Follow our [installation guide](/installation) to get Auth.js installed in your app.

## Features

- Built in support for popular services (Google, Facebook, Auth0, Apple…)
- Built-in support for databases/ORMs (MySQL, Postgres, Prisma, Drizzle…)
- Built-in email/passwordless/magic link authentication
- Use with _any_ [OAuth 2](https://www.ietf.org/rfc/rfc6749.html) or [OpenID Connect](https://openid.net/specs/openid-connect-core-1_0.html) provider
- Use with _any_ username/password store

### Flexible

- Runtime agnostic - run anywhere! Vercel Edge Functions, Node.js, Serverless, etc.
- Use with any modern framework! Next.js, SolidStart, SvelteKit, etc.
- [Bring Your Own Database](/getting-started/adapters) - or none! MySQL, Postgres, MSSQL, MongoDB, etc. Choose database sessions or JWT.

### Secure by default

- Signed, prefixed, server-only cookies
- Built-in CSRF protection
- Doesn't rely on client-side JavaScript
- Encrypted JWT sessions

## Credits

Auth.js is an open-source project that is only possible [thanks to contributors](/contributors).

To financially support the development of Auth.js, you can check our [OpenCollective](https://opencollective.com/nextauth) page. We appreciate your support 💚.
